version: '3.4'

services:
  protoc-gen-angular:
    image: protoc-gen-angular
    build:
      context: ./
    volumes:
      - type: bind
        source: ./release
        target: /protoc-gen-angular/release
        volume:
          nocopy: true
